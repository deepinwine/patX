# patX - 专利数据管理系统 C++/Rust 极速版

## 项目概述

将 Python 版专利数据管理系统 (patentmanager-pyside6) 迁移至 C++/Rust 混合架构，实现极致性能提升。

### 性能目标
- 核心检索速度提升 50-100 倍
- 批量导入速度提升 20-50 倍
- 并发处理能力提升 10 倍以上
- 支持千万级专利数据

### 架构设计
```
┌─────────────────────────────────────────────────────────┐
│                   CLI/API 接口层                         │
├─────────────────────────────────────────────────────────┤
│              C++/Rust FFI 交互适配层                     │
├──────────────────────┬──────────────────────────────────┤
│   Rust 安全并发层    │      C++ 性能核心层              │
│  - 并发调度          │  - 内存索引                      │
│  - 内存池管理        │  - 底层 IO                       │
│  - 数据校验          │  - 序列化                        │
│  - 任务编排          │  - 数据持久化                    │
├──────────────────────┴──────────────────────────────────┤
│                   数据持久化层 (SQLite)                  │
└─────────────────────────────────────────────────────────┘
```

## 技术栈

### C++ (底层性能核心)
- 标准: C++17/C++20
- 编译器: GCC/Clang
- 优化: -O3 -flto -march=native
- 依赖: Abseil, SQLite3, mmap, FlatBuffers

### Rust (安全并发中间层)
- 版本: Rust 1.70+
- 依赖: rustc_hash, crossbeam, 内存池

## 核心模块

### 1. 专利数据结构 (C++)
```cpp
struct Patent {
    char geke_code[16];        // 格科编码
    char application_no[32];   // 申请号
    char title[256];           // 发明名称
    char applicant[128];       // 申请人
    char application_date[16]; // 申请日期
    char patent_type[32];      // 专利类型
    char legal_status[32];     // 法律状态
    // ... 其他字段
};
```

### 2. 内存索引 (C++)
- 专利号索引: flat_hash_map<O(1)>
- 申请号索引: flat_hash_map<O(1)>
- 组合索引: 多条件快速检索

### 3. 并发调度 (Rust)
- 无锁线程池
- 任务队列
- 批量任务编排

### 4. 数据持久化
- SQLite3 原生接口
- 零拷贝序列化
- 内存映射 IO

## 迁移步骤

### Phase 1: 基础框架
- [ ] 项目结构搭建
- [ ] C++ 核心数据结构
- [ ] Rust 并发框架
- [ ] FFI 接口定义

### Phase 2: 核心功能
- [ ] 数据库管理模块
- [ ] 专利 CRUD 操作
- [ ] 批量导入/导出
- [ ] 数据检索模块

### Phase 3: 高级功能
- [ ] PDF 解析
- [ ] Excel 处理
- [ ] 统计分析
- [ ] 数据校验

### Phase 4: 性能优化
- [ ] 内存索引优化
- [ ] 并发优化
- [ ] IO 优化
- [ ] 编译优化

## 原项目文件清单

| 文件 | 功能 | 行数 |
|------|------|------|
| main.py | 主程序+UI | ~3000 |
| db_manager.py | 数据库管理 | ~1500 |
| pdf_parser.py | PDF解析 | ~200 |
| import_handlers.py | 导入处理 | ~300 |
| sw_dram.py | 软著管理 | ~300 |

## 兼容性要求

1. 数据格式兼容 Python 版
2. SQLite 数据库结构一致
3. 配置文件格式兼容
4. 操作流程保持一致
